<div id="error">
  <h1>403 Forbidden</h1>
  <p>
  <%=
    case @exception
    when ApplicationController::IpAddressRejected
      "あなたのIPアドレスからは（#{request.ip}）利用できません。"
    else
      "指定されてたページを閲覧する権限がありません。"
    end
  %>
  </p>
</div>
